import { css, DefaultTheme } from "styled-components";

const colors = {
    gray100: "#D9D9D9",
    while: "#FFFFFF",
};

const fonts = {};

const theme = {
    colors,
    fonts,
};

export default theme;